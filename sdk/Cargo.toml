[package]
name = "sovereign-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
sovereign-core = { path = "../core" }
tokio = { version = "1.38.0", features = ["full"] }
async-trait = "0.1.80"
thiserror = "1.0.61"

[dev-dependencies]
rand = "0.8.5"
